# SADNxAI LLM Evaluation Framework Dependencies
#
# REQUIRES: Python 3.10 or higher
#   (Uses modern type hints: list[str], dict[str, Any], X | None)
#
# Install with:
#   pip install -r evals/requirements.txt
#
# Or if using the main project:
#   pip install -r requirements.txt  (includes these)

# Async HTTP client for API calls
httpx>=0.25.0,<1.0.0
